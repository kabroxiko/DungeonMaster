// Deprecated: the HTTP API is gmai-server (Go). From the repository root run: npm start
console.error(
  'This entry point is deprecated. Run the API with Go: npm start (from the repo root).'
);
process.exit(1);
