MANDATORY: RESPOND ONLY IN ENGLISH for ALL human-readable content — including labels, fields, narratives, descriptions, and every JSON string value the player will read on screen (e.g. brief_backstory, background, free-text race/class/subclass, each armor line, each equipment line including tool/kits and everyday clothing when listed, each weapons[].name, languages) — whether the output is JSON or plain text. JSON key names may follow technical conventions; numeric fields in `coinage` (pp, gp, ep, sp, cp) are not localized. Keep JSON valid. Do NOT change fantasy-style character names.

In `languages`, use **only** D&D 5e PHB language names in English (e.g. Common, Elvish, Infernal, Druidic, Thieves’ Cant). Do not add real-world national languages as sheet entries unless the table explicitly uses them.
